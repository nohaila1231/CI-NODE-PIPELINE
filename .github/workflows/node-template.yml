name: Node.js CI/CD Template

on:
  workflow_call:
    inputs:
      # Core configuration
      node-versions:
        description: 'JSON array of Node.js versions to test (e.g., ["18.x", "20.x", "22.x"])'
        required: false
        default: '["20.x"]'
        type: string

      working-directory:
        description: 'Working directory for all commands'
        required: false
        default: '.'
        type: string

      package-manager:
        description: 'Package manager to use (npm, yarn, pnpm, bun)'
        required: false
        default: 'npm'
        type: string

      # Build configuration
      install-command:
        description: 'Custom install command (overrides default based on package manager)'
        required: false
        default: ''
        type: string

      build-command:
        description: 'Build command to run'
        required: false
        default: 'npm run build'
        type: string

      # Testing configuration
      test-command:
        description: 'Test command to run'
        required: false
        default: 'npm test'
        type: string

      enable-coverage:
        description: 'Enable code coverage reporting'
        required: false
        default: 'false'
        type: string

      coverage-command:
        description: 'Custom coverage command'
        required: false
        default: 'npm run coverage'
        type: string

      coverage-threshold:
        description: 'Minimum coverage percentage required (0-100)'
        required: false
        default: '80'
        type: string

      # Linting & quality
      enable-linting:
        description: 'Enable linting checks'
        required: false
        default: 'true'
        type: string

      lint-command:
        description: 'Lint command to run'
        required: false
        default: 'npm run lint'
        type: string

      enable-type-checking:
        description: 'Enable TypeScript type checking'
        required: false
        default: 'false'
        type: string

      type-check-command:
        description: 'Type check command to run'
        required: false
        default: 'npm run type-check'
        type: string

      # Caching
      enable-cache:
        description: 'Enable dependency caching'
        required: false
        default: 'true'
        type: string

      cache-key-prefix:
        description: 'Prefix for cache keys'
        required: false
        default: 'node-deps'
        type: string

      # Artifacts
      enable-artifacts:
        description: 'Upload build artifacts'
        required: false
        default: 'false'
        type: string

      artifact-path:
        description: 'Path to artifacts directory'
        required: false
        default: 'dist'
        type: string

      artifact-retention-days:
        description: 'Number of days to retain artifacts'
        required: false
        default: '5'
        type: string

      # Pre/Post scripts
      pre-install-script:
        description: 'Script to run before install'
        required: false
        default: ''
        type: string

      post-install-script:
        description: 'Script to run after install'
        required: false
        default: ''
        type: string

      pre-build-script:
        description: 'Script to run before build'
        required: false
        default: ''
        type: string

      post-build-script:
        description: 'Script to run after build'
        required: false
        default: ''
        type: string

      # Behavior controls
      fail-on-warnings:
        description: 'Treat linting warnings as errors'
        required: false
        default: 'false'
        type: string

      continue-on-error:
        description: 'Continue workflow even if tests fail'
        required: false
        default: 'false'
        type: string

      # Publishing
      publish-to-npm:
        description: 'Publish to npm registry after successful test'
        required: false
        default: 'false'
        type: string

      npm-registry:
        description: 'NPM registry URL'
        required: false
        default: 'https://registry.npmjs.org/'
        type: string

      # Release Management (Phase 1)
      enable-release:
        description: 'Enable automated release management with semantic versioning'
        required: false
        default: 'false'
        type: string

      release-branch:
        description: 'Branch to trigger releases from (typically main or master)'
        required: false
        default: 'main'
        type: string

      git-user-name:
        description: 'Git user name for release commits'
        required: false
        default: 'GitHub Actions'
        type: string

      git-user-email:
        description: 'Git user email for release commits'
        required: false
        default: 'actions@github.com'
        type: string

      # Security Scanning (Phase 3)
      enable-security-scan:
        description: 'Enable security scanning (npm audit, CodeQL)'
        required: false
        default: 'false'
        type: string

      security-fail-on-error:
        description: 'Fail workflow on security vulnerabilities'
        required: false
        default: 'false'
        type: string

      # Multi-Environment Support (Phase 4)
      enable-deployment:
        description: 'Enable deployment jobs'
        required: false
        default: 'false'
        type: string

      target-environment:
        description: 'Target deployment environment (dev, staging, prod)'
        required: false
        default: 'dev'
        type: string

      deployment-command:
        description: 'Command to run for deployment'
        required: false
        default: ''
        type: string

      # Concurrency Control (Phase 6)
      enable-concurrency:
        description: 'Enable concurrency control (cancel in-progress runs)'
        required: false
        default: 'true'
        type: string

      # Notifications (Phase 8)
      enable-notifications:
        description: 'Enable workflow notifications'
        required: false
        default: 'false'
        type: string

      notify-on-success:
        description: 'Send notification on success'
        required: false
        default: 'false'
        type: string

      notify-on-failure:
        description: 'Send notification on failure'
        required: false
        default: 'true'
        type: string

      slack-webhook-url:
        description: 'Slack webhook URL for notifications'
        required: false
        default: ''
        type: string

    secrets:
      npm-token:
        description: 'NPM authentication token for private packages'
        required: false

      npm-registry-token:
        description: 'Token for publishing to npm'
        required: false

      git-token:
        description: 'GitHub token for creating releases and tags'
        required: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

env:
  # Disable CI environment flag to prevent unnecessary checks
  CI: true

jobs:
  # Job: Validate inputs
  validate:
    name: Validate Configuration
    runs-on: ubuntu-latest
    steps:
      - name: Validate node versions JSON
        run: |
          echo "Node versions: ${{ inputs.node-versions }}"
          echo '${{ inputs.node-versions }}' | jq '.' > /dev/null || {
            echo "❌ Invalid node-versions JSON: ${{ inputs.node-versions }}"
            exit 1
          }

      - name: Validate package manager
        run: |
          case "${{ inputs.package-manager }}" in
            npm|yarn|pnpm|bun) echo "✓ Valid package manager: ${{ inputs.package-manager }}" ;;
            *) echo "❌ Invalid package manager: ${{ inputs.package-manager }}" && exit 1 ;;
          esac

      - name: Validate coverage threshold
        run: |
          THRESHOLD="${{ inputs.coverage-threshold }}"
          if ! [[ "$THRESHOLD" =~ ^[0-9]+$ ]] || [ "$THRESHOLD" -lt 0 ] || [ "$THRESHOLD" -gt 100 ]; then
            echo "❌ Coverage threshold must be 0-100, got: $THRESHOLD"
            exit 1
          fi
          echo "✓ Coverage threshold valid: $THRESHOLD%"

  # Job: Setup and test
  test:
    name: Test (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    needs: validate
    continue-on-error: ${{ inputs.continue-on-error == 'true' }}

    strategy:
      fail-fast: false
      matrix:
        node-version: ${{ fromJson(inputs.node-versions) }}

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Pre-install script
      - name: Run pre-install script
        if: inputs.pre-install-script != ''
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.pre-install-script }}

      # Setup Node.js
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: ${{ inputs.npm-registry }}

      # Setup package manager (pnpm, bun, yarn)
      - name: Setup package manager
        run: |
          case "${{ inputs.package-manager }}" in
            pnpm)
              npm install -g pnpm
              echo "Package manager: pnpm $(pnpm --version)"
              ;;
            bun)
              npm install -g bun
              echo "Package manager: bun $(bun --version)"
              ;;
            yarn)
              npm install -g yarn
              echo "Package manager: yarn $(yarn --version)"
              ;;
            npm)
              echo "Package manager: npm $(npm --version)"
              ;;
          esac

      # Configure cache
      - name: Configure cache
        if: inputs.enable-cache == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: ${{ inputs.package-manager == 'bun' && '' || inputs.package-manager }}
          cache-dependency-path: ${{ inputs.working-directory }}/package-lock.json

      # Generate custom cache key for bun (not supported by setup-node)
      - name: Generate bun cache key
        if: inputs.enable-cache == 'true' && inputs.package-manager == 'bun'
        id: bun-cache
        run: |
          CACHE_KEY="${{ inputs.cache-key-prefix }}-bun-${{ hashFiles(format('{0}/bun.lockb', inputs.working-directory)) }}"
          echo "key=$CACHE_KEY" >> $GITHUB_OUTPUT

      # Restore bun cache
      - name: Restore bun cache
        if: inputs.enable-cache == 'true' && inputs.package-manager == 'bun'
        uses: actions/cache@v3
        with:
          path: ~/.bun/install/cache
          key: ${{ steps.bun-cache.outputs.key }}
          restore-keys: |
            ${{ inputs.cache-key-prefix }}-bun-

      # Install dependencies
      - name: Install dependencies
        working-directory: ${{ inputs.working-directory }}
        run: |
          if [ -n "${{ inputs.install-command }}" ]; then
            echo "Running custom install command..."
            ${{ inputs.install-command }}
          else
            case "${{ inputs.package-manager }}" in
              pnpm) pnpm install --frozen-lockfile ;;
              yarn) yarn install --frozen-lockfile ;;
              bun) bun install --frozen-lockfile ;;
              npm) npm ci ;;
            esac
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.npm-token }}

      # Post-install script
      - name: Run post-install script
        if: inputs.post-install-script != ''
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.post-install-script }}

      # Pre-build script
      - name: Run pre-build script
        if: inputs.pre-build-script != ''
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.pre-build-script }}

      # Linting
      - name: Run linting
        if: inputs.enable-linting == 'true'
        working-directory: ${{ inputs.working-directory }}
        continue-on-error: ${{ inputs.fail-on-warnings != 'true' }}
        run: ${{ inputs.lint-command }}

      # Type checking
      - name: Run type checking
        if: inputs.enable-type-checking == 'true'
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.type-check-command }}

      # Build
      - name: Build project
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.build-command }}

      # Post-build script
      - name: Run post-build script
        if: inputs.post-build-script != ''
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.post-build-script }}

      # Test
      - name: Run tests
        working-directory: ${{ inputs.working-directory }}
        continue-on-error: ${{ inputs.continue-on-error == 'true' }}
        run: ${{ inputs.test-command }}

      # Coverage
      - name: Generate coverage report
        if: inputs.enable-coverage == 'true'
        working-directory: ${{ inputs.working-directory }}
        continue-on-error: true
        run: ${{ inputs.coverage-command }}

      # Upload coverage
      - name: Upload coverage to Codecov
        if: inputs.enable-coverage == 'true'
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/coverage-final.json
          flags: node-${{ matrix.node-version }}
          fail_ci_if_error: false
     
      - name: build
        run: npm run build

      # Upload artifacts
      - name: Upload artifacts
        if: inputs.enable-artifacts == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.node-version }}
          path: ${{ inputs.working-directory }}/${{ inputs.artifact-path }}
          retention-days: ${{ inputs.artifact-retention-days }}

  # Job: Publish to npm (optional)
  publish:
    name: Publish to NPM
    runs-on: ubuntu-latest
    needs: test
    if: inputs.publish-to-npm == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: ${{ inputs.npm-registry }}

      - name: Setup package manager
        run: |
          case "${{ inputs.package-manager }}" in
            pnpm)
              npm install -g pnpm
              ;;
            bun)
              npm install -g bun
              ;;
            yarn)
              npm install -g yarn
              ;;
          esac

      - name: Install dependencies
        working-directory: ${{ inputs.working-directory }}
        run: |
          if [ -n "${{ inputs.install-command }}" ]; then
            ${{ inputs.install-command }}
          else
            case "${{ inputs.package-manager }}" in
              pnpm) pnpm install --frozen-lockfile ;;
              yarn) yarn install --frozen-lockfile ;;
              bun) bun install --frozen-lockfile ;;
              npm) npm ci ;;
            esac
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.npm-token }}

      - name: Build project
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.build-command }}

      - name: Publish to npm
        working-directory: ${{ inputs.working-directory }}
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.npm-registry-token }}

  # Job: Security scanning (Phase 3)
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: test
    if: inputs.enable-security-scan == 'true'
    continue-on-error: ${{ inputs.security-fail-on-error == 'false' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run npm audit
        working-directory: ${{ inputs.working-directory }}
        run: |
          echo "Running npm audit..."
          npm audit --audit-level=moderate || true
        continue-on-error: ${{ inputs.security-fail-on-error == 'false' }}

      - name: Setup Node.js for CodeQL
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: 'javascript'

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  # Job: Calculate semantic version and create release (Phase 1-2)
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: test
    if: |
      inputs.enable-release == 'true' &&
      github.ref == format('refs/heads/{0}', inputs.release-branch) &&
      github.event_name == 'push'
    outputs:
      version: ${{ steps.semver.outputs.next-version }}
      should-release: ${{ steps.semver.outputs.should-release }}
      git-tag: ${{ steps.semver.outputs.git-tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - name: Copy scripts from template
        run: |
          mkdir -p scripts
          cp -r ${{ github.action_path }}/scripts/* scripts/
          ls -la scripts/

      - name: Calculate semantic version
        id: semver
        run: |
          node scripts/semantic-version.js

      - name: Configure Git
        run: |
          git config user.name "${{ inputs.git-user-name }}"
          git config user.email "${{ inputs.git-user-email }}"

      - name: Generate changelog
        if: steps.semver.outputs.should-release == 'true'
        working-directory: ${{ inputs.working-directory }}
        run: |
          node "${{ github.action_path }}/scripts/changelog-generator.js" "${{ steps.semver.outputs.next-version }}"

      - name: Create Git tag
        if: steps.semver.outputs.should-release == 'true'
        run: |
          git tag "${{ steps.semver.outputs.git-tag }}"
          git push origin "${{ steps.semver.outputs.git-tag }}"
        env:
          GITHUB_TOKEN: ${{ secrets.git-token || secrets.GITHUB_TOKEN }}

      - name: Commit changelog
        if: steps.semver.outputs.should-release == 'true'
        run: |
          git add CHANGELOG.md || true
          git commit -m "chore: update changelog for ${{ steps.semver.outputs.next-version }}" || true
          git push origin "${{ inputs.release-branch }}"
        env:
          GITHUB_TOKEN: ${{ secrets.git-token || secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        if: steps.semver.outputs.should-release == 'true'
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.git-token || secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.semver.outputs.git-tag }}
          release_name: Release ${{ steps.semver.outputs.next-version }}
          draft: false
          prerelease: false

      - name: Output release information
        if: steps.semver.outputs.should-release == 'true'
        run: |
          echo "✓ Release created:"
          echo "  Version: ${{ steps.semver.outputs.next-version }}"
          echo "  Tag: ${{ steps.semver.outputs.git-tag }}"
          echo "  URL: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.semver.outputs.git-tag }}"

  # Job: Deploy to environment (Phase 4-5)
  deploy:
    name: Deploy to ${{ matrix.environment }}
    runs-on: ubuntu-latest
    needs: test
    if: inputs.enable-deployment == 'true'
    strategy:
      matrix:
        environment: ['dev', 'staging', 'prod']
    environment:
      name: ${{ matrix.environment }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Setup package manager
        run: |
          case "${{ inputs.package-manager }}" in
            pnpm)
              npm install -g pnpm
              ;;
            bun)
              npm install -g bun
              ;;
            yarn)
              npm install -g yarn
              ;;
          esac

      - name: Install dependencies
        working-directory: ${{ inputs.working-directory }}
        run: |
          if [ -n "${{ inputs.install-command }}" ]; then
            ${{ inputs.install-command }}
          else
            case "${{ inputs.package-manager }}" in
              pnpm) pnpm install --frozen-lockfile ;;
              yarn) yarn install --frozen-lockfile ;;
              bun) bun install --frozen-lockfile ;;
              npm) npm ci ;;
            esac
          fi

      - name: Build project
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.build-command }}

      - name: Deploy to environment
        if: inputs.deployment-command != ''
        working-directory: ${{ inputs.working-directory }}
        run: ${{ inputs.deployment-command }}

      - name: Notify deployment success
        if: inputs.enable-notifications == 'true' && inputs.notify-on-success == 'true'
        run: |
          node "${{ github.action_path }}/scripts/slack-notifier.js" deployment \
            '{"deploymentEnv":"${{ matrix.environment }}","workflowName":"${{ github.workflow }}","runNumber":"${{ github.run_number }}","runUrl":"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}'
        env:
          SLACK_WEBHOOK_URL: ${{ inputs.slack-webhook-url }}
        continue-on-error: true

  # Job: Workflow notifications (Phase 8)
  notify:
    name: Send Notifications
    runs-on: ubuntu-latest
    needs: [test, security, release, deploy]
    if: always() && inputs.enable-notifications == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Determine workflow status
        id: status
        run: |
          if [ "${{ needs.test.result }}" = "failure" ] || [ "${{ needs.security.result }}" = "failure" ]; then
            echo "status=failure" >> $GITHUB_OUTPUT
          else
            echo "status=success" >> $GITHUB_OUTPUT
          fi

      - name: Send failure notification
        if: steps.status.outputs.status == 'failure' && inputs.notify-on-failure == 'true'
        run: |
          node "${{ github.action_path }}/scripts/slack-notifier.js" failure \
            '{"workflowName":"${{ github.workflow }}","runNumber":"${{ github.run_number }}","runUrl":"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}","branch":"${{ github.ref_name }}","commit":"${{ github.sha }}","author":"${{ github.actor }}"}'
        env:
          SLACK_WEBHOOK_URL: ${{ inputs.slack-webhook-url }}
        continue-on-error: true

      - name: Send success notification
        if: steps.status.outputs.status == 'success' && inputs.notify-on-success == 'true'
        run: |
          node "${{ github.action_path }}/scripts/slack-notifier.js" success \
            '{"workflowName":"${{ github.workflow }}","runNumber":"${{ github.run_number }}","runUrl":"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}","branch":"${{ github.ref_name }}","commit":"${{ github.sha }}","author":"${{ github.actor }}"}'
        env:
          SLACK_WEBHOOK_URL: ${{ inputs.slack-webhook-url }}
        continue-on-error: true
